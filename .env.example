MOCK_MODE=true
MOCK_SCENARIO=all_clear
DATABASE_URL=sqlite:///./data/dashboard.db
DASHBOARD_SECRET_KEY=change-me-in-production

# Optional: require X-API-Key header on API routes (leave unset for open/trusted-LAN mode)
# DASHBOARD_API_KEY=

# Status history tier defaults (override at runtime in Settings → Status history)
# STATUS_HISTORY_RAW_DAYS=30
# STATUS_HISTORY_HOURLY_DAYS=90

# Bind on all interfaces for internal LAN access (default 0.0.0.0)
# BIND_HOST=0.0.0.0

# systemd: sudo ./scripts/setup-systemd.sh --install-dir "$(pwd)" --user "$(whoami)"

# Production: set MOCK_MODE=false, use openssl rand -hex 32 for DASHBOARD_SECRET_KEY
# CORS_ORIGINS=["https://dashboard.example.com"]
# FRONTEND_STATIC_DIR=/opt/dashboard/frontend/dist
# DISCOVERY_MAX_TARGETS=512